1、dui xml 可拖动范围：	<Window size="574,442" roundcorner="5,5" caption="0,0,0,42">
	 
2、时间：	time_t curr_tm = 0; 
			time(&curr_tm);
			if (curr_tm - m_lastsendjstime > 1)
	
3、cef remote debugging :

	重加载：	location.reload()
	长度：		this.options.answers.length
			this.$("span.active")
4、jQuery：	
	$(this).hide() - 隐藏当前元素
	$("p").hide() - 隐藏所有 <p> 元素
	$("p.test").hide() - 隐藏所有 class="test" 的 <p> 元素
	$("#test").hide() - 隐藏所有 id="test" 的元素
	$(".intro")	class="intro" 的所有元素